<ui:ContentDialog x:Class="GestRehema.Views.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:GestRehema.Views"
             Title="Attention"
                  PrimaryButtonText="Oui"
                  CloseButtonText="Non"
                  DefaultButton="Primary"
                  PrimaryButtonCommand="{Binding SaveCustomer}"
                  IsPrimaryButtonEnabled="{Binding IsValid}"
             mc:Ignorable="d" >
    <Grid>
        <TextBlock x:Name="TxtMessage" Text="Voulez- vous vraiment supprimer cet article?" FontSize="16"/>
    </Grid>
</ui:ContentDialog>
